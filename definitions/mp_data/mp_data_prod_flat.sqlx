config {
  type: "table",
  description: "Data about the members of parliament in flat format"
}

SELECT
  * EXCEPT(updated_time)
FROM
  ${ref("backup_mp_data_prod_flat")}
WHERE
  updated_time = (
    SELECT
      MAX(updated_time)
    FROM
      ${self()}
  )
